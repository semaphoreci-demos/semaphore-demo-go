version: v1.0
name: Go project
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Build project
    task:
      jobs:
      - name: Docker build
        commands:
          - checkout
          - echo "Docker build..."

  - name: Tests
    task:
      jobs:
      - name: Test packages
        commands:
          - checkout
          - echo "Testing Go Packages"

  - name: Integration Tests
    task:
      jobs:
      - name: RSpec
        commands:
          - checkout
          - echo "Testing Integration"

      - name: Check security
        commands:
          - checkout
          - echo "make security"

  - name: Push Image to Docker Hub
    task:
      jobs:
      - name: Login and Push
        commands:
          - checkout
          - echo "docker login"
          - echo "docker push"

